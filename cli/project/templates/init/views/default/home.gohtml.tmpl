{{define "content"}}
<div class="welcome">
  <h2>{{.message}}</h2>
  <p>Get started by editing <code>controllers/default.go</code></p>
  
  <div class="actions">
    <h3>Next steps:</h3>
    <ul>
      <li>Create a new controller: <code>tracks generate controller get /about</code></li>
      <li>Create a new resource: <code>tracks generate resource posts</code></li>
      <li>Set up your database: <code>tracks db migrate</code></li>
    </ul>
  </div>
</div>
{{end}}